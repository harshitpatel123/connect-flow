type AuthResponse {
  accessToken: String!
}

type CurrentUser {
  id: ID!
  email: String!
}

type Query {
  getUser: CurrentUser!
}

type Mutation {
  register(email: String!, password: String!): AuthResponse!
  login(email: String!, password: String!): AuthResponse!
}
