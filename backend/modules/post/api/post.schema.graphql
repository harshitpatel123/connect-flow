type Post {
  id: ID!
  userId: Int!
  content: String!
  createdAt: String!
  user: User!
}

type User {
  id: ID!
  email: String!
}

type Query {
  myPosts: [Post!]!
  postsByIds(ids: [ID!]!): [Post!]!
}

type Mutation {
  createPost(content: String!): Post!
}
